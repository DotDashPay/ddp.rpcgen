/**
THIS FILE IS AUTOGENERATED: CHANGES WILL BE OVERWRITTEN

{{ name|lower }}.js contains the public API for calling {{ name|lower }}-related requests.
**/

var server = require("../server");
// Request imports
{% for request in requests %}
var {{ request.name }}Request = require("./{{request.name}}Request");
{% endfor %}


{% for request in requests %}
module.exports.{{ request.name|lowercase_first_letter }} = function {{ request.name|lowercase_first_letter }}({{ request.argument.name|lowercase_first_letter }}) {
    var request = new {{ request.name }}Request({{ request.argument.name|lowercase_first_letter }});
    server.sendRequest(request);
    return request;
}

{% endfor %}
